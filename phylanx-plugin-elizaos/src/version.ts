// =============================================================================
// src/version.ts — plugin version exported as a single constant.
// Bumped in lockstep with package.json by `npm version`.
// =============================================================================

export const PLUGIN_VERSION = "0.12.0";
