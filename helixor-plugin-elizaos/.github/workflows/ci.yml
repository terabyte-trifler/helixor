name: Plugin CI

on:
  push:
    paths: ["helixor-plugin-elizaos/**"]
  pull_request:
    paths: ["helixor-plugin-elizaos/**"]

jobs:
  test:
    runs-on: ubuntu-latest
    defaults: { run: { working-directory: helixor-plugin-elizaos } }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "20", cache: "npm", cache-dependency-path: "helixor-plugin-elizaos/package-lock.json" }

      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
      - run: npm run build

      - uses: actions/upload-artifact@v4
        with:
          name: helixor-plugin-dist-${{ github.sha }}
          path: helixor-plugin-elizaos/dist/
